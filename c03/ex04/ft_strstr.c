char    *ft_strstr(char *str, char *to_find)
{
    int i;

    i = 0;
    if (to_find[] = '\0')
        return (str);

    while (str[i] != '\0')
    {
        while (str[i] == to_find[i])
        {
            
        }
    }
}
